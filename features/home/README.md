# Home feature

The homepage is built from the frozen database contract. Recent incident content will be connected to the existing public incident view when the incidents data-access layer is implemented. No database schema changes belong in this feature.
