"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function CreateStaffForm(){const router=useRouter();const [authUserId,setAuthUserId]=useState("");const [displayName,setDisplayName]=useState("");const [role,setRole]=useState("moderator");const [loading,setLoading]=useState(false);const [message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setMessage("");try{const res=await fetch("/api/admin/staff",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({operation:"create",authUserId,displayName,role})});const data=await res.json().catch(()=>null);if(!res.ok){setMessage(data?.message||"স্টাফ যোগ করা যায়নি।");return}setMessage("স্টাফ সফলভাবে যোগ হয়েছে।");setAuthUserId("");setDisplayName("");router.refresh()}catch{setMessage("সার্ভারে যোগাযোগ করা যায়নি।")}finally{setLoading(false)}}return <form onSubmit={submit} className="mb-6 rounded-3xl border border-zinc-200 bg-white p-6 shadow-sm dark:border-zinc-800 dark:bg-zinc-900"><h2 className="text-base font-bold">নতুন স্টাফ যোগ করুন</h2><p className="mt-1 text-xs text-zinc-500">Auth-এ আগে তৈরি থাকা ব্যবহারকারীর ID এখানে দিন।</p><div className="mt-4 grid gap-4 md:grid-cols-3"><input required value={authUserId} onChange={e=>setAuthUserId(e.target.value)} placeholder="Auth user ID" className="h-11 rounded-xl border px-3 text-sm dark:border-zinc-700 dark:bg-zinc-800"/><input required minLength={2} maxLength={100} value={displayName} onChange={e=>setDisplayName(e.target.value)} placeholder="নাম" className="h-11 rounded-xl border px-3 text-sm dark:border-zinc-700 dark:bg-zinc-800"/><select value={role} onChange={e=>setRole(e.target.value)} className="h-11 rounded-xl border px-3 text-sm dark:border-zinc-700 dark:bg-zinc-800"><option value="moderator">মডারেটর</option><option value="admin">অ্যাডমিন</option></select></div>{message&&<p className="mt-3 text-xs text-zinc-600 dark:text-zinc-400">{message}</p>}<button disabled={loading} className="mt-4 h-10 rounded-xl bg-zinc-950 px-4 text-xs font-semibold text-white disabled:opacity-40 dark:bg-white dark:text-zinc-950">{loading?"যোগ করা হচ্ছে…":"স্টাফ যোগ করুন"}</button></form>}
